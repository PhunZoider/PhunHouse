VERSION = 1,

option PhunSafePaint.MinDistanceBetweenSafehouses
{ 
    type = integer, 
    default = 25,
    min = 0,
    max = 100,
    page = PhunSafePaint,
    translation = PhunSafePaint_MinDistanceBetweenSafehouses ,
}

option PhunSafePaint.MinSizeForNew
{ 
    type = integer, 
    default = 9,
    min = 0,
    max = 100,
    page = PhunSafePaint,
    translation = PhunSafePaint_MinSizeForNew,
}

option PhunSafePaint.MaxNumberOfOwned
{ 
    type = integer, 
    default = 1,
    min = 0,
    max = 1000,
    page = PhunSafePaint,
    translation = PhunSafePaint_MaxNumberOfOwned,
}

option PhunSafePaint.Consumption
{ 
    type = integer, 
    default = 1,
    min = 0,
    max = 1000,
    page = PhunSafePaint,
    translation = PhunSafePaint_Consumption,
}

option PhunSafePaint.MaxTotalArea
{ 
    type = integer, 
    default = 100,
    min = 0,
    max = 10000,
    page = PhunSafePaint,
    translation = PhunSafePaint_MaxTotalArea,
}

option PhunSafePaint.AdminsIgnoreRestrictions
{ 
    type = boolean, 
    default = true,
    page = PhunSafePaint,
    translation = PhunSafePaint_AdminsIgnoreRestrictions,
}